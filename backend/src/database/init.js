import { db } from "./db.js"; export async function initDatabase() { await db.query(` CREATE TABLE IF NOT EXISTS usuarios ( id SERIAL PRIMARY KEY, nome VARCHAR(100) NOT NULL, email VARCHAR(150) UNIQUE NOT NULL, senha TEXT NOT NULL, criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP ) `); await db.query(` CREATE TABLE IF NOT EXISTS receber ( id SERIAL PRIMARY KEY, usuario_id INTEGER REFERENCES usuarios(id), descricao VARCHAR(150) NOT NULL, valor NUMERIC(10,2) NOT NULL, vencimento DATE NOT NULL, status VARCHAR(20) DEFAULT 'pendente', observacao TEXT, criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP ) `); await db.query(` CREATE TABLE IF NOT EXISTS pagar ( id SERIAL PRIMARY KEY, usuario_id INTEGER REFERENCES usuarios(id), descricao VARCHAR(150) NOT NULL, valor NUMERIC(10,2) NOT NULL, vencimento DATE NOT NULL, status VARCHAR(20) DEFAULT 'pendente', observacao TEXT, criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP ) `); await db.query(` CREATE TABLE IF NOT EXISTS historico ( id SERIAL PRIMARY KEY, usuario_id INTEGER REFERENCES usuarios(id), tipo VARCHAR(20) NOT NULL, descricao VARCHAR(150) NOT NULL, valor NUMERIC(10,2) NOT NULL, data_movimento TIMESTAMP DEFAULT CURRENT_TIMESTAMP ) `); }